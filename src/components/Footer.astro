---
import ButtonArrow from "./ButtonArrow.astro";
---

<footer>
  <div class="grid_1-2 footer_width">
    <div class="footer_left">
      <div>
        <h2>Kontaktinformation</h2>
        <p>Telefon nr.: <a href="tel:+4548301932">48 30 19 32</a></p>
        <p>
          E-mailadresse: <a href="mailto:info@bakkefest.dk">info@bakkefest.dk</a
          >
        </p>
        <p>
          Adresse: Gilleleje Hallen / Festpladsen <br />Bøgebakken 19C, 3250
          Gilleleje
        </p>
        <p class="CVR">CVR. nr.: 59173715</p>
      </div>
      <div>
        <h2>Åbningstider</h2>
        <p>Mandag – fredag: 8.00 – 15.30</p>
        <p>Weekend efter aftale</p>
      </div>
    </div>

    <div class="footer_right">
      <div>
        <h2>Send os en besked!</h2>
        <p>
          Skriv dit navn og e-mailadresse herunder samt en besked indeholdende
          din forspørgsel. Udfyld venligst hele kontaktformularen.
        </p>
      </div>

      <div>
        <form action="/send-message" method="POST">
          <div class="grid_1-1">
            <div>
              <label for="name"></label>
              <input
                type="text"
                id="name"
                name="name"
                placeholder="Fulde navn"
                required
              />
            </div>
            <div>
              <label for="phone"></label>
              <input
                type="tel"
                id="phone"
                name="phone"
                placeholder="Telefonnummer"
                required
              />
            </div>
          </div>
          <label for="message"></label>
          <textarea
            id="message"
            name="message"
            placeholder="Besked"
            rows="5"
            required></textarea>
          <div class="email">
            <div class="grid_1-1">
              <div class="email_input">
                <label for="email"></label>
                <input
                  type="email"
                  id="email"
                  name="email"
                  placeholder="E-MAIL"
                  required
                />
              </div>
              <div class="button_send">
                <input type="submit" value="SEND" class="button" />
                <ButtonArrow
                  url="/send-message"
                  background_color="var(--yellow)"
                  arrow_color="var(--green)"
                />
              </div>
            </div>
          </div>
          <div class="line"></div>
        </form>
      </div>
    </div>
  </div>
  <div class="some_info footer_width">
    <h4>Følg os på sociale medier!</h4>
    <div class="img_some">
      <a href="https://www.instagram.com/bakkefest" target="_blank">
        <img src="/assets/instagram.svg" alt="Instagram ikon" />
      </a>
      <a href="https://www.facebook.com/Bakkefesten/" target="_blank">
        <img src="/assets/facebook.svg" alt="Facebook ikon" />
      </a>
    </div>
  </div>
</footer>

<style>
  form {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  /* input::placeholder styrer det indhold som står inde i boksen FØR MAN BEGYNDER AT SKRIVE */
  input::placeholder,
  textarea::placeholder {
    color: var(--pink);
    opacity: 0.5;
    font-family: neue-haas-grotesk-display, sans-serif;
    font-weight: 500;
    font-style: normal;
    font-size: 0.875rem;
  }

  input,
  textarea {
    font-family: neue-haas-grotesk-display, sans-serif;
    color: var(--pink);
    font-weight: 500;
    font-style: normal;
    font-size: 0.875rem;
    text-align: left; /* Justering af teksten */
  }

  input,
  textarea,
  button {
    width: 100%;
    padding: 8px;
    border: none;
    border-radius: 5px;
  }

  /* her har jeg ændret indput feltet ved at tage fat først i input(generelle) så i type=email - som referer til boksen som automatisk er omkring email inputtet.*/
  input[type="email"] {
    font-size: 1.5rem;
    color: var(--pink);
    background: none;
    border: none;
    text-align: left;
    outline: none;
    padding: 0;
  }

  /* Herefter har jeg taget fat i type=email igen, men denne gang i placeholder, så jeg kan style teksten! Da den er anderledes end de andre indput felter */
  input[type="email"]::placeholder {
    font-size: 1.25rem;
    color: var(--light_pink);
    font-family: owners-narrow, sans-serif;
    font-weight: 700;
    font-style: normal;
    opacity: 1;
  }

  /* samme for submit aka send knappen - skulle også have en anden styling*/
  input[type="submit"] {
    color: var(--light_pink);
    background: none;
    border: none;
    text-align: right;
    outline: none;
    padding: 0;
    font-family: owners-narrow, sans-serif;
    font-weight: 700;
    font-style: normal;
    font-size: 1.25rem;
  }
  input[type="submit"]::placeholder {
    font-size: 1.25rem;
    color: var(--light_pink);
    font-family: owners-narrow, sans-serif;
    font-weight: 700;
    font-style: normal;
    opacity: 1;
  }

  button {
    color: var(--light_pink);
    border: none;
  }
  button:hover {
    background-color: var(--pink);
  }
  p {
    color: var(--light_pink);
  }

  h2,
  h4,
  a {
    color: var(--light_pink);
  }

  h2,
  h4 {
    text-transform: uppercase;
  }

  .CVR {
    margin-top: 15px;
    margin-bottom: 30px;
  }

  a:hover {
    color: var(--pink);
  }

  img:hover {
    transform: scale(1.1);
    transition: all 0.3s ease;
  }

  footer {
    background-color: var(--green);
    padding-top: 15px;
    padding-bottom: 30px;
  }

  .grid_1-1 {
    display: grid;
    grid-template-columns: 1fr;
    gap: 20px;
  }
  .email .grid_1-1 {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
    margin-top: 8px;
  }

  .email_input {
    align-items: center;
    margin-top: 6px;
  }

  .line {
    height: 1px;
    background-color: var(--light_pink);
    width: auto;
  }

  .some_info {
    margin-top: 45px;
  }
  .img_some {
    display: flex;
    flex-direction: row;
    padding: 10px 0;
    gap: 10px;
  }
  .img_some img {
    width: 50px;
    height: 50px;
    transition: all 0.3s ease;
  }

  .grid_1-2 {
    display: grid;
    grid-template-columns: 1fr;
    gap: 20px;
    padding-top: 45px;
  }
  .footer_left {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }
  .footer_right {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .button_send {
    display: flex;
    flex-direction: row;
    justify-content: start;
    gap: 10px;
  }

  @media (min-width: 800px) {
    .grid_1-1 {
      gap: 20px;
      grid-template-columns: 1fr 1fr;
    }

    .grid_1-2 {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 20px;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 10px;
      max-width: 100%;
      margin: 0 auto;
    }

    button {
      padding: 30px;
    }
    .footer_right {
      display: flex;
      align-items: right;
      flex-direction: column;
      gap: 20px;
    }
    .footer_right h2 {
      text-align: right;
    }
    .footer_right p {
      text-align: right;
    }

    input::placeholder,
    textarea::placeholder {
      font-size: 1.125rem;
      line-height: 1.7rem;
    }
    .line {
      margin-top: -5px;
    }

    input,
    textarea {
      font-size: 1.125rem;
    }
  }
</style>
