---
const { title, content } = Astro.props;
---

<div class="accordion-wrapper">
  <details class="accordion">
    <summary>
      <span class="title-text">
        <h3>{title}</h3>
      </span>
      <span class="plus_icon">+</span>
    </summary>
    <div class="content">
      <p>{content}</p>
    </div>
  </details>
</div>

<style>
  .accordion-wrapper {
    margin: 0 auto;
    padding: 1rem 0;
  }

  details.accordion {
    border: none;
    padding: 0.2rem 0;
  }

  /* <details class="accordion">: Et standard HTML-element, der selv kan vise og skjule indhold, når man klikker på overskriften – uden brug af JavaScript. Skaber mindre kode = mere bæredygtighed */

  summary {
    display: flex;
    justify-content: space-between;
    cursor: pointer;
    font-weight: bold;
    font-size: 1rem;
    margin-bottom: 0.2rem;
    border-bottom: 1px solid var(--yellow);
    padding-bottom: 0.2rem;
    transition: 0.3s ease;
    width: 100%;
  }
  /* <summary>: Det klikbare element inde i <details>, som fungerer som kontrolknap. Når brugeren klikker her, toggles (åbnes/lukkes) det underliggende indhold automatisk af browseren. */

  /* - display: flex; justify-content: space-between; bruges til at placere teksten til venstre og plus-ikonet til højre. */

  /* - border-bottom og padding-bottom skaber en visuel adskillelse mellem hvert accordion-element. */

  .plus_icon {
    margin-left: 1rem;
    font-weight: bold;
    font-size: 1.2rem;
    transition: 0.3s ease;
    color: var(--yellow);
  }

  details[open] .plus_icon {
    transform: rotate(45deg);
    transition: 0.3s ease;
  }

  /* - .plus_icon: Viser "+"-ikon, som roteres 45° ved åbning vha. CSS-selector details[open]. */

  /* - Farver og overgangseffekter styres af CSS-variabler og transition: 0.3s ease for glidende effekter. */

  .content {
    padding: 0.5rem 0 0.5rem 0;
    font-size: 18px;
    line-height: 1.7rem;
    color: var(--light_pink);
  }

  h3 {
    font-family: owners-narrow, sans-serif;
    font-size: 30px;
    font-weight: 700;
    font-style: normal;
    color: var(--yellow);
  }
  /* <h3>: Overskriften for hvert accordion-panel, lagt i <span> for struktur. */

  h3:hover {
    transition: 0.3s;
  }
</style>
